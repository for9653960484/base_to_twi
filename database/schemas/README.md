# Альтернативная SQL-схема (без pgvector)

Используется **только** локально через `scripts/init_database.py`, когда расширение pgvector недоступно.

Не размещать в `database/init/` — Docker применяет все файлы из `init/` при первом запуске.
